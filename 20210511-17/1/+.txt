Документация проверяется по ./../Состояния_Документация.txt
Проверка производится только класса
\VinKekFish\vinkekfish\keccak\keccak-20200918\Keccak_PRNG_20201128.cs

0. Проверить, что состояния объектов где-то документированы и ссылка на документацию указана в исходниках как относительный путь к файлу (или, если это удобнее, как абсолютный путь от корня проекта).

Документация лежит в файле .\Documentation\Keccak_PRNG_20201128.md (по отношению к директории проверяемого файла)
Ошибка: Ссылка на документацию в исходниках не указана

В файле документации указаны другие найденные ошибки.


Файлы проверяются по файлу
./../Состояния_Проверка.txt

1. Выписать все объекты, которые используются в ходе программы

1.0. Все объекты проверяемого класса
Выписаны в файл
Keccak_PRNG_20201128.objects.cs

В ходе проверки из класса Keccak_abstract убрано неиспользуемое поле ulong d


1.1. Все объекты, которые глобально (вне одной функции) используются проверяемым участком программы
Кажется, таких объектов нет, кроме сборщика мусора и статических методов

2. Все объекты, которые локально (внутри функции) используются в программе: пройти по всем функциям и заранее выписать эти объекты в отдельный список, идущий сразу после функции, чтобы потом не забыть

Выписано в проверяемый файл

1. Для всех выписанных объектов указать их начальные состояния, если их сразу можно выяснить (если нет, то это будет выясняться далее в WMv05ZkV8lkD).
Указаны в Keccak_PRNG_20201128.objects.states


В BytesBuilderForPointers.Record.Dispose
	первая строка изменена на
		bool allocatorExists = allocator != null || array != null;
	// Добавлен array != null



lfQFibWa0cRr. Составить граф вызовов функций
Данный пункт проверки пропущен для упрощения.


cFNNZfO63pYL. Для каждого объекта также необходимо указать
Дополнительно указано в Keccak_PRNG_20201128.objects.cFNNZfO63pYL
